// Not using wildcard syntax to make final dependencies more obvious
export {
    TimeSync,
    newReadonlyDate,
    REFRESH_IDLE,
    REFRESH_ONE_HOUR,
    REFRESH_ONE_MINUTE,
    REFRESH_ONE_SECOND,
    type SubscriptionHandshake,
} from "./TimeSync"